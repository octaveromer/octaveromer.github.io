/* =====================================================
   CLÉ DE L'AGENT IA (Gemini Flash — gratuit)
   =====================================================
   1) Récupère ta clé gratuite ici : https://aistudio.google.com/apikey
   2) Colle-la entre les guillemets ci-dessous.
   3) IMPORTANT : restreins la clé à ton site (voir README) pour
      qu'elle ne soit pas utilisable ailleurs même si elle est visible.
   ===================================================== */
window.GEMINI_API_KEY = "";
