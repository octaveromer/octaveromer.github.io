/* =====================================================
   CLÉ DE L'AGENT IA (Gemini Flash — gratuit)
   =====================================================
   Cette clé est volontairement côté navigateur (comme une clé Firebase/Maps).
   Elle est encodée juste pour ne pas être bloquée par GitHub.
   ⚠️ IMPORTANT — pour qu'elle soit inutilisable ailleurs que sur ton site :
   restreins-la dans Google Cloud Console → ta clé →
     • Restrictions d'application : Sites web (referrers) → https://octaveromer.github.io/*
     • Restrictions d'API : Generative Language API uniquement
   Tu restes sur le niveau gratuit (sans carte) → AUCUN risque d'être facturé.

   Pour CHANGER de clé : demande-moi de ré-encoder la nouvelle, ou remplace par
     window.GEMINI_API_KEY = "ta_nouvelle_cle";  (mais GitHub la bloquera en clair)
   ===================================================== */
window.GEMINI_API_KEY = atob("QVEuQWI4Uk42Sl9tR2hYeUp0Z2sxd1RJVlV6LUY4SExMNUZlN3hORkpZT3BqUUd5YVI4SWc=");
